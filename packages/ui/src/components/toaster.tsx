"use client";
import { Toaster } from "sonner";

/** Pre-configured Toaster for admin pages. Position bottom-right. */
export function AdminToaster() {
  return <Toaster position="bottom-right" />;
}
